# דיפלוי AURA ל-Railway — צ'ק-ליסט

## 0. הכנה מקומית

צרו את הסיסמה המוצפנת ואת סוד הסשן, שמרו אותם בצד:

```bash
npm run hash-password -- "הסיסמה-החזקה-שלך"
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"   # SESSION_SECRET
```

## 1. קוד ל-GitHub

```bash
cd aura
git init                 # אם עוד לא
git add .
git commit -m "AURA"
git remote add origin https://github.com/<user>/aura.git
git branch -M main
git push -u origin main
```

> `.gitignore` כבר מונע העלאת `.env`, `node_modules`, `aura.db` — הסודות לא עולים.

## 2. פרויקט ב-Railway

1. [railway.app](https://railway.app) → התחברות עם GitHub.
2. **New Project → Deploy from GitHub repo → aura**.
3. Railway מריץ `npm start` אוטומטית (מוגדר ב-`railway.json`).

## 3. דיסק קבוע ל-DB (חובה)

Settings → **Volumes → New Volume** → Mount path: `/data`.
בלי זה כל הלקוחות נמחקים בכל דיפלוי.

## 4. משתני סביבה (Variables)

| משתנה | ערך |
|---|---|
| `DATA_DIR` | `/data` |
| `ADMIN_PASSWORD_HASH` | הפלט של hash-password (מתחיל ב-`scrypt$`) |
| `SESSION_SECRET` | המחרוזת האקראית |
| `ANTHROPIC_API_KEY` | — |
| `GREEN_API_ID_INSTANCE` | — |
| `GREEN_API_TOKEN` | — |
| `OPENAI_API_KEY` | — |
| `OPENAI_RESPONSES_MODEL` | `gpt-5.6` (לאמת בחשבון OpenAI) |
| `BYTEPLUS_API_KEY` | — |
| `SEEDREAM_MODEL` / `SEEDANCE_MODEL` | מזהי המודלים שלך |
| `TZ_NAME` | `Asia/Jerusalem` |
| `SCHEDULED_CONTENT_ENABLED` | `true` כדי להפעיל תוכן מתוזמן. **ברירת המחדל בקוד היא `false`** — בלי המשתנה הזה לא נשלח תוכן מתוזמן כלל |

> אין להוסיף `ADMIN_PASSWORD` ולא `PORT` — Railway מזריק `PORT` לבד, וה-hash מחליף את הסיסמה הגלויה.

## 4.1 תוכן מתוזמן — שלושה שערים

הפיצ'ר שולח רק אם **כל השלושה** מתקיימים. שכחה של אחד מהם היא הסיבה הנפוצה ל"לא נשלח כלום":

| # | שער | איפה מדליקים |
|---|---|---|
| 1 | `SCHEDULED_CONTENT_ENABLED=true` | משתנה סביבה (הטבלה למעלה). הערך חייב להיות `true` — `1` לא יעבוד. דורש הפעלה מחדש |
| 2 | המתזמן לא ב-pause | כפתור בפאנל הניהול (המצב נשמר ב-DB ושורד דיפלוי) |
| 3 | `scheduled_content_enabled` ללקוח | תיבת סימון בכרטיס הלקוח. **כבוי כברירת מחדל לכל לקוח** |

בנוסף הלקוח חייב להיות ב-`status = 'active'`.

> **שעת השליחה בפורמט 24 שעות** (`22:09`, לא `10:09PM`). ערך פגום נופל אוטומטית ל-07:30.

### התאוששות מתקלה — עד סוף היום

לקוח שלא קיבל את התוכן שלו בגלל תקלה **זכאי לקבל אותו כל עוד לא התחלף היום המקומי**.
עם שעת שליחה 09:00:

| שעה מקומית | נשלח? | |
|---|---|---|
| 08:30 | לא | לפני שעת השליחה |
| 09:05 | כן | בזמן |
| 13:00 | כן | חזרה מתקלה של 4 שעות — הלקוח שילם על היום הזה |
| 23:45 | כן | עדיין אותו יום |

התחלף היום? אי אפשר לשלוח באיחור — במקום זה היום **מזוכה חזרה למכסה** והלקוח מקבל הודעה.

### לוח שבועי לכל לקוח

כל לקוח מקבל בכרטיס שלו טבלה של 7 ימים × 3 סוגי תוכן. אפשר לקבוע, למשל,
קרוסלה ברביעי, סטורי אחד בשלישי ושלושה בחמישי.

הסכום השבועי מוגבל ל**מכסת החבילה** (`WEEKLY_ALLOWANCE` ב-`src/config.js`):

| חבילה | סטוריז/שבוע | קרוסלות/שבוע | רילז/שבוע |
|---|---|---|---|
| בסיסית | 14 | 1 | 1 |
| מורחבת | 28 | 2 | 2 |

אפשר לחלק **פחות** מהמכסה, לא יותר — הפאנל יחזיר שגיאה שמציינת איזה סוג חורג.
המחזורים הם **שבוע קלנדרי (ראשון–שבת)** באזור הזמן של הלקוח, כדי ש"קרוסלה ברביעי"
תהיה אותו יום לכל הלקוחות.

### לקוח חדש לא מקבל תוכן ביום ההרשמה

לקוח שנוצר — או שהודלק לו הדגל — **אחרי** שעת השליחה של אותו יום מקבל שורות בסטטוס `skipped`
לאותו יום. אף עובד לא נוגע בהן (`claimNext` בוחר רק `scheduled`) והן לא נספרות במכסה
(רק `delivered` נספר). הטיק הבא מוצא את המפתחות תפוסים ולא מכניס כלום לתור.
**השליחה מתחילה בשעת השליחה הבאה.**

אם בכל זאת רוצים לשלוח את היום שדולג — כפתור השליחה מחדש בפאנל מחזיר שורה `skipped` ל-`scheduled`.

## 5. פרסום ופתיחה

1. שמירת המשתנים → Railway מדפלוי מחדש.
2. Settings → Networking → **Generate Domain** → כתובת ציבורית עם HTTPS.
3. פותחים את הכתובת → כניסה עם הסיסמה → מזינים לקוח ראשון.

## 6. ווטסאפ

עובד ב-polling — מתחיל להאזין אוטומטית כשהשירות עולה. רק לוודא שה-Instance ב-Green API
מחובר למספר (סריקת QR). אין צורך ב-webhook.

## עדכונים

```bash
git add . && git commit -m "..." && git push
```

Railway מדפלוי לבד; ה-DB על ה-Volume נשמר (הלקוחות לא נמחקים).
