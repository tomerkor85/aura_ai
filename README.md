# AURA — סוכנת שיווק AI אישית בווטסאפ

מערכת שמייצרת תוכן שיווקי יומי וממותג לכל לקוח דרך שיחת ווטסאפ.
סוכן אחד משותף לכל הלקוחות — הזיהוי לפי מספר הטלפון, והתוכן (טקסט, תמונות, וידאו) נוצר לפי פרופיל המותג של אותו לקוח ב-DB.

## רכיבים

| רכיב | תפקיד |
|---|---|
| `src/index.js` | תהליך ראשי: מאזין להודעות ווטסאפ + מתזמן + פאנל ניהול |
| `src/admin.js` + `public/` | פאנל ניהול לקוחות (UI) — הזנת פרופיל מותג, שמירה ל-DB, "צור תוכן עכשיו" |
| `src/agent.js` | הסוכן: Claude לטקסט + כלים `generate_image` (create/edit) / `generate_video` |
| `src/openai_images.js` | תמונות בצ'אט — OpenAI Responses API (יצירה + עריכה המשכית) |
| `src/visual.js` | תמונות יומיות (Seedream) + וידאו (Seedance), OpenAI כגיבוי |
| `src/daily.js` | יצירה ושליחה של התוכן היומי לכל לקוח לפי חבילה |
| `src/greenapi.js` | שכבת ווטסאפ (שליחה, קבלה ב-polling) |
| `src/auth.js` | אבטחת פאנל הניהול (hash סיסמה, סשן, rate-limit) |

## תמונות בצ'אט — יצירה ועריכה המשכית

הלקוח כותב בווטסאפ, ו-Claude מזהה אם זו יצירה חדשה או עריכה של התמונה האחרונה:

- "תכין לי תמונה של תרנגול" → תמונה חדשה (`mode=create`).
- "תגדיל" / "רקע כחול של המותג" / "תעשה אותו יותר שמח" → עריכה של התמונה הקודמת (`mode=edit`),
  דרך OpenAI Responses API עם `previous_response_id` — שומר על אותו נושא, סגנון וצבעי מותג.
- אם לא ברור אם רוצים חדש או עריכה — Claude שואל את הלקוח קודם.

לכל לקוח נשמר `last_response_id` + snapshot של פרופיל המותג מזמן היצירה (טבלת `image_state`),
כדי שעריכות עתידיות יישארו עקביות גם אם הפרופיל השתנה.

תמונות **יומיות** (מהמתזמן) הן יצירה חד-פעמית ולכן משתמשות ב-Seedream. וידאו — Seedance.

## התקנה והרצה

```bash
cd aura
npm install
cp .env.example .env    # ולמלא מפתחות
npm start               # מאזין לווטסאפ + מתזמן יומי + פאנל ניהול
```

פאנל הניהול: http://localhost:3000 — מזינים סיסמה, מוסיפים לקוח, ממלאים פרופיל מותג, שומרים.
כפתור "צור תוכן עכשיו" מייצר ושולח מיד לווטסאפ.

בדיקות:

```bash
npm run smoke                       # בדיקת עשן בלי קריאות API
npm run send-daily                  # שליחת תוכן יומי לכל הלקוחות עכשיו
npm run send-daily -- 972501234567  # רק ללקוח מסוים
```

## מפתחות (`.env`)

| משתנה | מאיפה |
|---|---|
| `GREEN_API_ID_INSTANCE` + `GREEN_API_TOKEN` | Green API — Instance מחובר למספר ווטסאפ |
| `ANTHROPIC_API_KEY` | מנוע הטקסט (Claude) |
| `OPENAI_API_KEY` + `OPENAI_RESPONSES_MODEL` | תמונות בצ'אט (Responses API) |
| `BYTEPLUS_API_KEY` + `SEEDREAM_MODEL` + `SEEDANCE_MODEL` | תמונות יומיות ווידאו |
| `ADMIN_PASSWORD_HASH` + `SESSION_SECRET` | אבטחת פאנל הניהול |

## אבטחת פאנל הניהול

מנהל יחיד, סיסמה אחת, אבל מוקשח לרשת: סיסמה כ-scrypt hash, סשן בטוקן חתום ב-cookie
(`HttpOnly` + `SameSite=Strict` + `Secure`), והגבלת קצב על ניסיונות כניסה.

לפרודקשן:

```bash
npm run hash-password -- "סיסמה-חזקה-שלך"
# מעתיקים את הפלט ל-ADMIN_PASSWORD_HASH ב-.env, מוחקים את ADMIN_PASSWORD.
```

וגם מגדירים `SESSION_SECRET` למחרוזת אקראית ארוכה.

## חבילות

| | סטוריז/יום | קרוסלות/שבוע | וידאו |
|---|---|---|---|
| `basic` | 2 | 1 (ראשון) | — |
| `premium` | 4 | 2 (ראשון + רביעי) | ✓ |

## דיפלוי לענן

ראו [DEPLOY.md](DEPLOY.md) — צ'ק-ליסט מלא ל-Railway (דיסק קבוע, משתני סביבה, דומיין).
