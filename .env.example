# --- Green API (WhatsApp) ---
GREEN_API_ID_INSTANCE=
GREEN_API_TOKEN=

# --- Text engine provider: 'openai' (default) or 'anthropic' ---
# Switch the brain between OpenAI and Claude. Images always use OpenAI.
TEXT_PROVIDER=openai

# --- Anthropic (Claude) - only needed if TEXT_PROVIDER=anthropic ---
ANTHROPIC_API_KEY=

# --- BytePlus (visual engine: Seedream images + Seedance video) ---
BYTEPLUS_API_KEY=
# Region endpoint for BytePlus ModelArk (Ark). Default is the AP-Southeast host.
BYTEPLUS_BASE_URL=https://ark.ap-southeast.bytepluses.com/api/v3
# Model / endpoint IDs from your BytePlus console:
SEEDREAM_MODEL=seedream-3-0-t2i-250415
SEEDANCE_MODEL=seedance-1-0-lite-t2v-250428

# --- OpenAI ---
# Used for: (1) conversational/editable images in chat via the Responses API,
#           (2) fallback for daily one-shot images.
OPENAI_API_KEY=
OPENAI_IMAGE_MODEL=gpt-image-1
# Model for the Responses API (editable multi-turn images). Confirm the exact
# name in your OpenAI account.
OPENAI_RESPONSES_MODEL=gpt-5.6
# Model for the chat/text engine (copy, hooks, tool calling).
OPENAI_TEXT_MODEL=gpt-5.6

# --- Visual provider selection ---
# byteplus (default) or openai — which provider generates images
IMAGE_PROVIDER=byteplus

# --- Behavior ---
# Attach generated images to each daily story automatically (true/false).
DAILY_IMAGES=false
# Timezone for the daily scheduler
TZ_NAME=Asia/Jerusalem

# --- Admin panel ---
ADMIN_PORT=3000
# Password to open the admin UI.
# PRODUCTION: run `npm run hash-password -- "your-password"` and set ADMIN_PASSWORD_HASH
# instead of the plaintext line below (then delete ADMIN_PASSWORD).
ADMIN_PASSWORD=change-me
ADMIN_PASSWORD_HASH=
# Secret for signing login sessions. Generate a long random string.
# If unset, sessions reset on every restart.
SESSION_SECRET=
# Force the Secure flag on the session cookie (set true if behind HTTPS and the
# proxy header isn't detected). On Railway HTTPS is automatic.
FORCE_SECURE_COOKIE=false

# --- Storage ---
# Directory for the SQLite DB. On Railway, point this at a mounted Volume (e.g. /data).
DATA_DIR=
